# For local split-origin web development. In the single-container Docker flow, SSR can
# fall back to BETTER_AUTH_URL and the browser uses window.location.origin.
VITE_API_URL=http://localhost:3000
# Set to true to bind the Vite dev server to all network interfaces.
#VITE_SERVER_HOST=true
